const initialState = {
  "Profile": "/",
  "Dialogs": "/dialogs",
  "Follows": "/follows",
  "News": "/news",
  "Settings": "/settings"
}

const pagesReducer = (state = initialState, action) => {
  return state
}

export default pagesReducer