const initialState = {
  "Profile": "/",
  "Dialogs": "/dialogs",
  "News": "/news",
  "Settings": "/settings"
}

const pagesReducer = (state = initialState, action) => {
  return state
}

export default pagesReducer